<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>My Wishlist</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <div class="container">
        <h1>My wishlist</h1>
        <form action="/wish" method="POST">
            <input type="text" name="userWish" placeholder="Add a wish" required>
            <button type="submit">Add a wish</button>
        </form>
        <div class="wishlist">
            <ul>
                <% if (wishes.length > 0) { %>
                    <% wishes.forEach(wish => { %>
                        <li><%= wish.description %></li>
                    <% }) %>
                <% } else { %>
                    <li>No wishes yet</li>
                <% } %>
            </ul>
        </div>
    </div>
</body>
</html>